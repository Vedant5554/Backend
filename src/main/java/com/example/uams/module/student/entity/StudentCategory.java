package com.example.uams.module.student.entity;

/**
 * Represents the category/type of a student.
 * Used in reports: "list students per category"
 */
public enum StudentCategory {
    UNDERGRADUATE,
    POSTGRADUATE,
    INTERNATIONAL
}